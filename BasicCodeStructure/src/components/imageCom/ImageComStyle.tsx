import { StyleSheet } from 'react-native';

import GLOBAL_STYLES from '@app/utils/globalStyles';

const styles = StyleSheet.create({
  container: {
    ...GLOBAL_STYLES.image20,
  },
  imageStyle: {
    ...GLOBAL_STYLES.image100Per,
  },
});

export default styles;
